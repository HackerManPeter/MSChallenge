version: '3.8'
services:
  api:
    build: .
    ports:
      - '3000:3000'
    .env_file:
      - .env
    depends_on:
      - mysql

  mysql:
    image: mysql
    volumes:
      - ./data:/docker-entrypoint-initdb.d
    ports:
      - '3306:3306'
    env_file: .env
